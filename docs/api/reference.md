# clow99/NexusShine — API Reference

Generated by NexusDocs from a scan of the repository at ref `main`.

## API Reference

### Endpoints

- `/api/auth/:...nextauth` — GET  
  Handles authentication routes (NextAuth).

- `/api/bathrooms` — GET, POST, PATCH, DELETE  
  Manage bathroom entities.

- `/api/branches` — GET, POST, PATCH, DELETE  
  Manage branch entities.

- `/api/clean` — POST  
  Handles cleaning operations.

- `/api/inspection` — POST  
  Handles inspection operations.

- `/api/inspection/clear` — POST  
  Clears inspection status.

- `/api/inspection/remind` — POST  
  Sends inspection reminders.

- `/api/locations` — GET, POST, PATCH, DELETE  
  Manage location entities.

- `/api/settings` — GET, PATCH  
  Manage application settings.

- `/api/tasks` — GET, POST, PATCH, DELETE  
  Manage task entities.

- `/api/theme` — PATCH  
  Change application theme.

- `/api/users` — GET, POST, PATCH, DELETE  
  Manage user entities.

- `/api/validate` — POST  
  Validates user codes.